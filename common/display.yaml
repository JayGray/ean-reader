display:
  - platform: lcd_gpio
    dimensions: 16x2
    data_pins:
      - GPIO12
      - GPIO13
      - GPIO14
      - GPIO15
    enable_pin: GPIO21
    rs_pin: GPIO22
    rw_pin: GPIO19
    lambda: |-
      it.print("Hello World!");
